<%= form_for(@product_batch) do |f| %>
  <% if @product_batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_batch.errors.count, "error") %> prohibited this product_batch from being saved:</h2>

      <ul>
      <% @product_batch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :worksheet %><br />
    <%= f.text_field :worksheet %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
