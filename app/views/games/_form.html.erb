<div class="well">
<%= simple_form_for(@game, :html => {:class => "form-vertical form-horizontal"}) do |f| %>

  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <div id="legend" class="">
      <legend class="">部署新游戏 - 第一步</legend>
    </div>
    
		<%= f.input :code, label: "游戏编码", :required => true, :input_html => {:class => 'input-xlarge'} %>
    <div class="control-group">
		</div>

  </fieldset>

  <div class="actions">
    <%= f.submit "下一步", class: "btn btn-success" %>
		<%= link_to '返回', games_path, class: "btn" %>
  </div>
<% end %>
</div>